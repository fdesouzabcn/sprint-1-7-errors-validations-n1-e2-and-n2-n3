# Practicing Errors and Validations.
  **Description**: In this practice (S1.07) we will have to practice some Errors and Validations.

  ## 🛠 Technologies
  - Frontend: PHP
  - Backend: PHP

  ## 🚀 Installation
  1. Clone the repository: N/A (`example: git clone ...`)
  2. Install dependencies: N/A (`example: npm install`)
  3. Environment Variables: N/A (`example: Create ``.env`` with...`)

  ## 📸 Demo
 N/A (`example:  [Link to Vercel/Netlify] or screenshots.`)
